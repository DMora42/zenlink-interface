{
  "name": "@grass-protocol/wagmi-config",
  "version": "0.0.0",
  "description": "Grass Wagmi Config",
  "author": "zenlinkpro",
  "license": "LGPL-2.1-or-later",
  "keywords": [
    "zenlink-interface",
    "wagmi",
    "config"
  ],
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "source": "./index.ts",
  "typings": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup index.ts --format esm,cjs --dts",
    "dev": "tsup index.ts --format esm,cjs --watch --dts",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "dependencies": {
    "@grass-protocol/chain": "workspace:*"
  },
  "devDependencies": {
    "@grass-protocol/typescript-config": "workspace:*",
    "tsup": "^7.1.0",
    "typescript": "5.1.6"
  }
}
